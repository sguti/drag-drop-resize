import { DraggableDirective } from "./draggable.directive";

describe("GDraggableDirective", () => {
  it("should create an instance", () => {
    // const directive = new DraggableDirective();
    // expect(directive).toBeTruthy();
  });
});
